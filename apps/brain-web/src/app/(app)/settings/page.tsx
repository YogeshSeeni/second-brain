import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "brain / settings" };

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="settings"
      note="OAuth scopes, cron schedules, build info — lands Day 2 evening."
    />
  );
}
