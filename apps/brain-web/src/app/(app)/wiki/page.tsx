import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "brain / wiki" };

export default function WikiPage() {
  return (
    <PlaceholderPage
      title="wiki browser"
      note="rendered vault pages with wikilink resolution — lands Day 5."
    />
  );
}
